apiVersion: batch/v1
kind: Job
metadata:
  name: coreai-mcu-knowledge-pgv-dbmigration
  namespace: coreai-mcu
spec:
  template:
    spec:
      containers:
      - name: coreai-mcu-knowledge-pgv-dbmigration
        image: sndadpinfcr3401.azurecr.io/image/coreai-mcu-knowledge-pgv-dbmigration:0.1.13-alpha.556247 # {"$imagepolicy": "flux-config:coreai-mcu-knowledge-pgv-dbmigration-snd-03"}
        env:
        - name: POSTGRES_HOST
          value: ${SHARED_POSTGRES_SERVER_01}.postgres.database.azure.com
        - name: SCHEMA_NAME
          value: ${POSTGRES_SCHEMA_NAME}
        - name: POSTGRES_DB
          value: ${POSTGRES_DB}
        - name: POSTGRES_PORT
          value: ${POSTGRES_PORT}
        - name: SCHEMA_USERNAME
          value: AAG-Azure-ADP-coreai-mcu-snd3-PostgresDB_Writer
