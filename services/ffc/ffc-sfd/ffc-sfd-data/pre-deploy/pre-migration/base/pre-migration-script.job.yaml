apiVersion: batch/v1
kind: Job
metadata:
  name: ffc-sfd-data-pre-dbmigration
  namespace: flux-config
  labels:
    azure.workload.identity/use: "true"
    backstage.io/kubernetes-id: ffc-sfd-data
    backstage.io/kubernetes-team: ffc-sfd
spec:
  template:
    metadata:
      labels:
        azure.workload.identity/use: "true"
        backstage.io/kubernetes-id: ffc-sfd-data
        backstage.io/kubernetes-team: ffc-sfd
    spec:
      serviceAccountName: ${PLATFORM_DB_ADMIN}
      restartPolicy: Never
      containers:
      - name: ffc-sfd-data-pre-dbmigration
        imagePullPolicy: Always
