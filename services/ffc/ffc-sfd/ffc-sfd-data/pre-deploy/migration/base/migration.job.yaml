apiVersion: batch/v1
kind: Job
metadata:
  name: ffc-sfd-data-dbmigration
  namespace: ffc-sfd
  labels:
    azure.workload.identity/use: "true"
    backstage.io/kubernetes-id: ffc-sfd-data
    backstage.io/kubernetes-team: ffc-sfd
spec:
  template:
    metadata:
      labels:
        azure.workload.identity/use: "true"
        backstage.io/kubernetes-id: ffc-sfd-data
        backstage.io/kubernetes-team: ffc-sfd
    spec:
      serviceAccountName: ${TEAM_NAMESPACE}
      restartPolicy: Never
      containers:
      - name: ffc-sfd-data-dbmigration
        imagePullPolicy: Always
