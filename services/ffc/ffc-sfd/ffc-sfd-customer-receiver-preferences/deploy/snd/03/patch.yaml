content:
  apiVersion: helm.toolkit.fluxcd.io/v2beta1
  kind: HelmRelease
  metadata:
    name: ffc-sfd-customer-receiver-preferences
    namespace: flux-config
  spec:
    chart:
      spec:
        version: 0.1.0 # {"$imagepolicy": "flux-config:ffc-sfd-customer-receiver-preferences-snd-03:tag"}
    values:
      environment: production
      name: ffc-sfd-customer-receiver-preferences
      namespace: ffc-sfd
      image: sndadpinfcr3401.azurecr.io/image/ffc-sfd-customer-receiver-preferences:0.1.0 # {"$imagepolicy": "flux-config:ffc-sfd-customer-receiver-preferences-snd-03"}
      serviceAccount:
        name: ffc-sfd-customer-receiver-preferences
      labels:
        component: web
      container:
        imagePullPolicy: Always
      ingress:
        class: nginx
        endpoint: __INGRESS_ENDPOINT__
        server: snd3.adp.defra.gov.uk
      deployment:
        priorityClassName: default
      containerConfigMap:
        configServiceName: ${APPCONFIG_NAME}
        configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
        serviceMIClientId: ${clientId}
