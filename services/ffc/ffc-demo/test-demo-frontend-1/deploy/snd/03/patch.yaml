apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-demo-frontend-1
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-demo-frontend-1-snd-03:tag"}
  values:
    environment: production
    name: test-demo-frontend-1
    namespace: ffc-demo
    image: sndadpinfcr3401.azurecr.io/image/test-demo-frontend-1:0.1.0 # {"$imagepolicy": "flux-config:test-demo-frontend-1-snd-03"}
    serviceAccount:
      name: test-demo-frontend-1
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: test-demo-frontend-1
      server: snd3.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
