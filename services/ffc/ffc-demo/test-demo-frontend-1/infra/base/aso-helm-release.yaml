apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-demo-frontend-1-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-demo-frontend-1
    backstage.io/kubernetes-team: ffc-demo
spec:
  releaseName: test-demo-frontend-1-infra
  chart:
    spec:
      chart: test-demo-frontend-1-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: ffc-demo-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: ffc-demo
  values:
    namespace: ffc-demo
