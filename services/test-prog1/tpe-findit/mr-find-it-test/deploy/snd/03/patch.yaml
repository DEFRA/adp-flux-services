apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mr-find-it-test
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:mr-find-it-test-snd-03:tag"}
  values:
    environment: production
    name: mr-find-it-test
    namespace: tpe-findit
    image: sndadpinfcr3401.azurecr.io/image/mr-find-it-test:0.1.0 # {"$imagepolicy": "flux-config:mr-find-it-test-snd-03"}
    serviceAccount:
      name: mr-find-it-test
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
