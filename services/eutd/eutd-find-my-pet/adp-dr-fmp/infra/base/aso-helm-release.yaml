apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-dr-fmp-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: adp-dr-fmp
    backstage.io/kubernetes-team: eutd-find-my-pet
spec:
  releaseName: adp-dr-fmp-infra
  chart:
    spec:
      chart: adp-dr-fmp-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: eutd-find-my-pet-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: eutd-find-my-pet
  values:
    namespace: eutd-find-my-pet
