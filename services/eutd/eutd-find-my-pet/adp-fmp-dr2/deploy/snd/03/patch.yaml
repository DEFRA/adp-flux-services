apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-fmp-dr2
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:adp-fmp-dr2-snd-03:tag"}
  values:
    environment: production
    name: adp-fmp-dr2
    namespace: eutd-find-my-pet
    image: sndadpinfcr3401.azurecr.io/image/adp-fmp-dr2:0.1.0 # {"$imagepolicy": "flux-config:adp-fmp-dr2-snd-03"}
    serviceAccount:
      name: adp-fmp-dr2
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
