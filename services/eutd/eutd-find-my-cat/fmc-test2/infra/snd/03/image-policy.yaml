apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: fmc-test2-snd-03
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: fmc-test2
    backstage.io/kubernetes-team: eutd-find-my-cat
spec:
  imageRepositoryRef:
    name: fmc-test2
  policy:
    semver:
      range: '>=0.1.0-0'
