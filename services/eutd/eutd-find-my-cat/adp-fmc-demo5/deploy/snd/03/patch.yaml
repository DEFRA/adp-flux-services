apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-fmc-demo5
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:adp-fmc-demo5-snd-03:tag"}
  values:
    environment: production
    name: adp-fmc-demo5
    namespace: eutd-find-my-cat
    image: sndadpinfcr3401.azurecr.io/image/adp-fmc-demo5:0.1.0 # {"$imagepolicy": "flux-config:adp-fmc-demo5-snd-03"}
    serviceAccount:
      name: adp-fmc-demo5
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: adp-fmc-demo5
      server: snd3.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
