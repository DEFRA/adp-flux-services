apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-demo-test-35
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:adp-demo-test-35-snd-03:tag"}
  values:
    environment: production
    name: adp-demo-test-35
    namespace: eutd-trade-exports-core
    image: sndadpinfcr3401.azurecr.io/image/adp-demo-test-35:0.1.0 # {"$imagepolicy": "flux-config:adp-demo-test-35-snd-03"}
    serviceAccount:
      name: adp-demo-test-35
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
