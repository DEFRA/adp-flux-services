apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo-payment-service
  namespace: config-cluster-flux
spec:
  releaseName: ffc-demo-payment-service
  chart:
    spec:
      chart: ffc-demo-payment-service
      version: "4.2.9" # {"$imagepolicy": "config-cluster-flux:ffc-demo-payment-service:tag"}
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: ffc-demo-payment-service
        namespace: config-cluster-flux
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: ffc-demo
  values:
    aadWorkloadIdentity: true
    serviceAccount:
      name: ffc-demo
      roleArn: true
