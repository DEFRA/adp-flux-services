apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ffc-demo-claim-service-pre-deploy
  namespace: flux-config
spec:
  dependsOn:
    - name: ffc-demo-claim-service-aso
      namespace: flux-config
  sourceRef:
    kind: GitRepository
    name: services-repository
    namespace: flux-config
  path: ./services/ffc-demo/ffc-demo-claim-service/pre-deploy/${ENVIRONMENT}/${CLUSTER}
  interval: 5m
  timeout: 5m
  prune: true
  wait: true
  force: true
  postBuild:
    substitute:
      POSTGRES_SERVER: "${POSTGRES_SERVER}"