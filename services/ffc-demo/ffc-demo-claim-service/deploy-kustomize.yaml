apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ffc-demo-claim-service-deploy
  namespace: config-cluster-flux
spec:
  dependsOn:
    - name: ffc-demo-claim-service-pre-deploy
      namespace: config-cluster-flux
  sourceRef:
    kind: GitRepository
    name: services-repository
    namespace: config-cluster-flux
  path: ./services/ffc-demo/ffc-demo-claim-service/deploy/${ENVIRONMENT}/${CLUSTER}
  interval: 5m
  timeout: 5m
  prune: true
  wait: true
  force: true
  postBuild:
    substitute:
      ACR_NAME: "${ACR_NAME}"
      ENVIRONMENT: "${ENVIRONMENT}"
      CLUSTER: "${CLUSTER}"