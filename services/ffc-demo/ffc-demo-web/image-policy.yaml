apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ffc-demo-web
  namespace: flux-config
spec:
  imageRepositoryRef:
    name: ffc-demo-web
  policy:
    semver:
      range: '>=1.0.0'