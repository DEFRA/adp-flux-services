apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo-web
  namespace: flux-config
spec:
  chart:
    spec:
      version: "4.32.8" # {"$imagepolicy": "flux-config:ffc-demo-web-snd-01:tag"}
  values:
    environment: production
    name: ffc-demo-web
    namespace: ffc-demo
    image: sndadpinfcr1401.azurecr.io/image/ffc-demo-web:4.32.8 # {"$imagepolicy": "flux-config:ffc-demo-web-snd-01"}
    serviceAccount:
      name: ffc-demo-web
    labels:
      component: web
    container:
      imagePullPolicy: Always
      requestMemory: 50Mi
      requestCpu: 50m
      limitMemory: 200Mi
      limitCpu: 400m
    ingress:
      class: nginx
      endpoint: ffc-demo-web
      server: snd1.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
