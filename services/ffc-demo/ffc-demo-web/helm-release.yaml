apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ffc-demo-web
  namespace: config-cluster-flux
spec:
  releaseName: ffc-demo-web
  chart:
    spec:
      chart: ffc-demo-web
      version: "4.32.3" # {"$imagepolicy": "config-cluster-flux:ffc-demo-web:tag"}
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: ffc-demo-web
        namespace: config-cluster-flux
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: ffc-demo
  values:
    aadWorkloadIdentity: true
    serviceAccount:
      name: ffc-demo
      roleArn: true
