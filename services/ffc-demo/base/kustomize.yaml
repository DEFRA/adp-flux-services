apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  namespace: ffc-demo
spec:
  path: ./services/ffc-demo/${ENVIRONMENT}
  postBuild:
    substitute:
      NAMESPACE: "ffc-demo"
      ENVIRONMENT: "${ENVIRONMENT}"
      ACR_NAME: "${ACR_NAME}"   

