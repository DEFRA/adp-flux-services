apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ffc-demo-base
  namespace: config-cluster-flux
spec:
  sourceRef:
    kind: GitRepository
    name: services-repository
    namespace: config-cluster-flux
  interval: 5m
  timeout: 3m
  prune: true
  path: ./services/ffc-demo/${ENVIRONMENT}/${CLUSTER}
  postBuild:
    substitute:
      NAMESPACE: "ffc-demo"
      WORKLOAD_IDENTITY_ID: "a46a3dca-bf0e-4b30-ab10-96a144bee9a6"
      TEAM_NAME: "ffc-demo-team"
      TEAM_MI_CLIENT_ID: "2201bc81-ac6e-41c2-b37d-5dd7ed264b39"
      TEAM_RG: "sndadpinfrg1401"
      ENVIRONMENT: "${ENVIRONMENT}"
      ACR_NAME: "${ACR_NAME}"
      CLUSTER: "\"${CLUSTER}\""
      CLUSTER_OIDC: "${CLUSTER_OIDC}"
      TENANT_ID: "${TENANT_ID}" 
      SUBSCRIPTION_ID: "${SUBSCRIPTION_ID}"
      SUBSCRIPTION_NAME: "${SUBSCRIPTION_NAME}"
      SERVICEBUS_RG: "${SERVICEBUS_RG}"
      SERVICEBUS_NS: "${SERVICEBUS_NS}"
      INFRA_RG: "${INFRA_RG}"
      TEAM_MI_PREFIX: "${TEAM_MI_PREFIX}"
      POSTGRES_SERVER_RG: "${POSTGRES_SERVER_RG}"
      POSTGRES_SERVER: "${POSTGRES_SERVER}"