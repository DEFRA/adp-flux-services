apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: coreai-mcu-documents
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:coreai-mcu-documents-tst-01:tag"}
  values:
    environment: production
    name: coreai-mcu-documents
    namespace: crai-mcu
    image: tstadpinfcr1401.azurecr.io/image/coreai-mcu-documents:0.1.0 # {"$imagepolicy": "flux-config:coreai-mcu-documents-tst-01"}
    serviceAccount:
      name: coreai-mcu-documents
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
