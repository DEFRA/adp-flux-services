apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: coreai-mcu-email-processor-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: coreai-mcu-email-processor
    backstage.io/kubernetes-team: crai-mcu
spec:
  releaseName: coreai-mcu-email-processor-infra
  chart:
    spec:
      chart: coreai-mcu-email-processor-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: crai-mcu-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: crai-mcu
  values:
    namespace: crai-mcu
