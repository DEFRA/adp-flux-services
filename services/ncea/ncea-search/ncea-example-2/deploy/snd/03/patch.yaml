apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ncea-example-2
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:ncea-example-2-snd-03:tag"}
  values:
    environment: production
    name: ncea-example-2
    namespace: ncea-search
    image: sndadpinfcr3401.azurecr.io/image/ncea-example-2:0.1.0 # {"$imagepolicy": "flux-config:ncea-example-2-snd-03"}
    serviceAccount:
      name: ncea-example-2
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
