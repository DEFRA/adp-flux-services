apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ncea-examplar
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:ncea-examplar-snd-03:tag"}
  values:
    environment: production
    name: ncea-examplar
    namespace: ncea-search
    image: sndadpinfcr3401.azurecr.io/image/ncea-examplar:0.1.0 # {"$imagepolicy": "flux-config:ncea-examplar-snd-03"}
    serviceAccount:
      name: ncea-examplar
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
