apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-dotnet-topic
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.1 # {"$imagepolicy": "flux-config:test-dotnet-topic-snd-03:tag"}
  values:
    environment: production
    name: test-dotnet-topic
    namespace: fcp-demo
    image: sndadpinfcr3401.azurecr.io/image/test-dotnet-topic:0.1.1 # {"$imagepolicy": "flux-config:test-dotnet-topic-snd-03"}
    serviceAccount:
      name: test-dotnet-topic
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
