apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-backend-dotnet-queue
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:test-backend-dotnet-queue-snd-03:tag"}
  values:
    environment: production
    name: test-backend-dotnet-queue
    namespace: fcp-demo
    image: sndadpinfcr3401.azurecr.io/image/test-backend-dotnet-queue:0.1.0 # {"$imagepolicy": "flux-config:test-backend-dotnet-queue-snd-03"}
    serviceAccount:
      name: test-backend-dotnet-queue
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
