apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-backend-dotnet-queue-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: test-backend-dotnet-queue
    backstage.io/kubernetes-team: fcp-demo
spec:
  releaseName: test-backend-dotnet-queue-infra
  chart:
    spec:
      chart: test-backend-dotnet-queue-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: fcp-demo-helm-repo
        namespace: flux-config
  install:
    createNamespace: true
  interval: 5m0s
  targetNamespace: fcp-demo
  values:
    namespace: fcp-demo
