apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: adp-demo-etc-snd-03
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: adp-demo-etc
    backstage.io/kubernetes-team: fcp-demo
spec:
  imageRepositoryRef:
    name: adp-demo-etc
  policy:
    semver:
      range: '>=0.1.0-0'
