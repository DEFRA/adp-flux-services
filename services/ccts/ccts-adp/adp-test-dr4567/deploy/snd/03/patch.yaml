apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-test-dr4567
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:adp-test-dr4567-snd-03:tag"}
  values:
    environment: production
    name: adp-test-dr4567
    namespace: ccts-adp
    image: sndadpinfcr3401.azurecr.io/image/adp-test-dr4567:0.1.0 # {"$imagepolicy": "flux-config:adp-test-dr4567-snd-03"}
    serviceAccount:
      name: adp-test-dr4567
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
