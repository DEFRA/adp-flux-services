apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-demo-5555
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:adp-demo-5555-snd-03:tag"}
  values:
    environment: production
    name: adp-demo-5555
    namespace: ccts-adp
    image: sndadpinfcr3401.azurecr.io/image/adp-demo-5555:0.1.0 # {"$imagepolicy": "flux-config:adp-demo-5555-snd-03"}
    serviceAccount:
      name: adp-demo-5555
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
