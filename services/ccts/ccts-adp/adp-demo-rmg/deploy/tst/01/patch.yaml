apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: adp-demo-rmg
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:adp-demo-rmg-tst-01:tag"}
  values:
    environment: production
    name: adp-demo-rmg
    namespace: ccts-adp
    image: tstadpinfcr1401.azurecr.io/image/adp-demo-rmg:0.1.0 # {"$imagepolicy": "flux-config:adp-demo-rmg-tst-01"}
    serviceAccount:
      name: adp-demo-rmg
    container:
      imagePullPolicy: Always
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
