apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ADP-DEMO-DR-CAPS
  namespace: flux-config
spec:
  chart:
    spec:
      version: 0.1.0 # {"$imagepolicy": "flux-config:ADP-DEMO-DR-CAPS-snd-03:tag"}
  values:
    environment: production
    name: ADP-DEMO-DR-CAPS
    namespace: azure-dev-platform
    image: sndadpinfcr3401.azurecr.io/image/ADP-DEMO-DR-CAPS:0.1.0 # {"$imagepolicy": "flux-config:ADP-DEMO-DR-CAPS-snd-03"}
    serviceAccount:
      name: ADP-DEMO-DR-CAPS
    labels:
      component: web
    container:
      imagePullPolicy: Always
    ingress:
      class: nginx
      endpoint: __INGRESS_ENDPOINT__
      server: snd3.adp.defra.gov.uk
    deployment:
      priorityClassName: default
    containerConfigMap:
      configServiceName: ${APPCONFIG_NAME}
      configServiceMIClientId: ${APPCONFIG_MI_CLIENTID}
      serviceMIClientId: ${clientId}
