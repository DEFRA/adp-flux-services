apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ADP-DEMO-CAPS2-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ADP-DEMO-CAPS2
    backstage.io/kubernetes-team: azure-dev-platform
spec:
  releaseName: ADP-DEMO-CAPS2-infra
  chart:
    spec:
      chart: ADP-DEMO-CAPS2-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: azure-dev-platform-helm-repo
        namespace: flux-config
  install:
    createNamespace: "true"
  interval: 5m0s
  targetNamespace: azure-dev-platform
  values:
    namespace: azure-dev-platform
