apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ADP-DEMO-CAPS2-snd-03
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: ADP-DEMO-CAPS2
    backstage.io/kubernetes-team: azure-dev-platform
spec:
  imageRepositoryRef:
    name: ADP-DEMO-CAPS2
  policy:
    semver:
      range: '>=0.1.0'
