apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: pets-demo-11-infra
  namespace: flux-config
  labels:
    backstage.io/kubernetes-id: pets-demo-11
    backstage.io/kubernetes-team: azure-dev-platform
spec:
  releaseName: pets-demo-11-infra
  chart:
    spec:
      chart: pets-demo-11-infra
      version: 0.1.0
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: azure-dev-platform-helm-repo
        namespace: flux-config
  install:
    createNamespace: "true"
  interval: 5m0s
  targetNamespace: azure-dev-platform
  values:
    namespace: azure-dev-platform
